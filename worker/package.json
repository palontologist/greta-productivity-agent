{
  "name": "greta-proxy-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for Greta — holds API keys server-side",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.114.17"
  }
}
